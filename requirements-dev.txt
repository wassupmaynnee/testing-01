# Test/dev tooling. The runtime app deps live in requirements.txt; install both
# in CI:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=9.0.3,<10   # 9.0.3+ clears CVE-2025-71176
httpx>=0.27,<0.29       # FastAPI TestClient transport
ruff>=0.5,<1
pytest-cov>=5.0,<7
pip-audit>=2.7,<3
fakeredis>=2.23,<3   # rate-limit tests without a live Redis
